.idea/
.vscode/
.DS_Store
bin/
!dot_local/bin/

# Per-machine MCP server config (may contain tokens)
.mcp.json

# Per-machine Claude Code overrides
.claude/settings.local.json

# mkdocs build artefacts and plugin caches
site/
.cache/

# Local Python venvs for docs work
.venv*/

# Local standup daily-logs (auto-written by the global session-end hook).
# Standups are delivered to the pinned tracking issue, not committed — see CLAUDE.md.
/standups/
