# Daily Log - 15 Jun 2026

## Session Summaries

### Session @ 15:07 (branch: main)

- babdff1 feat(git): make 'git sync' alias squash-merge aware (#37)
- 91e16e3 docs: clarify self-update paths and silent-degradation diagnostic (#36)
- b23666f chore(brewfile): rename windsurf cask to devin-desktop (#35)
- 73e002b docs: add plist XML validation to required-checks list (11 -> 12) (#34)
- a9d48f4 feat(defaults-audit): list unreadable entries under --drift (#33)
- 1e35121 fix(pre-commit): preserve ggshield exit code so detected secrets block commits (#32)
- 67a9af6 feat(ci): validate plist templates with xmllint (#31)

### Session @ ~15:30 (branch: main) — drift paper-cuts + CI/PR ergonomics

Started from the deferred [chezmoi-drift-paper-cuts-followup.md](~/.claude/plans/chezmoi-drift-paper-cuts-followup.md) plan; expanded into a broader review of the bot-driven PR flow.

- 563dbd0 fix(drift): silence brew-extras false positives and ignore oh-my-zsh cache (#38)
  - tightened `chezmoi-drift-check` grep so `Would remove: /path` (brew cache cleanup) no longer counts as a managed-package "extra"
  - added `.oh-my-zsh/cache/**` to `.chezmoiignore` to stop `chezmoi apply` prompting on the runtime cache
- ee3a287 chore(ci): retire update-brew workflow (superseded by local brewup) (#39)
  - CI runner has no installed-version state, so the jq filter reported ~45 formulae as `n/a → version` on every run; PR #23 sat untouched for ~30 days as a result
  - dropped references from CLAUDE.md, README.md, docs/runbooks/recover-from-drift.md (incl. the silent-degradation `gh run list` example)
- 9c071f5 feat(ci): auto-rebase open PRs when main advances (#40)
  - new workflow `.github/workflows/auto-rebase.yml` rebases every open same-repo PR with `git rebase` + `git push --force-with-lease`
  - filters forks, comments on conflict instead of failing
  - documented limitation: `GITHUB_TOKEN` pushes don't re-trigger downstream CI (`required_status_checks.strict=true` covers correctness)
  - proven on its debut push: cleanly rebased PR #24 (oh-my-zsh bump open since May 18) onto a tree with #29-#40 in zero conflicts
- 8c1237e chore(ci): swap GITHUB_TOKEN for BOT_PAT in bot-driven workflows (#41)
  - all four bot-driven workflows now prefer `${{ secrets.BOT_PAT || secrets.GITHUB_TOKEN }}` — fallback-safe; merge is risk-free even with `BOT_PAT` unset
  - manual follow-up: create fine-grained PAT (Contents R/W, Pull requests R/W, Workflows R/W), save as repo secret `BOT_PAT`

Housekeeping (early session 2):

- closed PR #23 (`chore: weekly brew outdated report`) and deleted its stale branch — companion to #39
- flipped repo settings: `allow_auto_merge=true`. Confirmed all merge modes still match intent: squash-only at the bottom (no merge-commit, no rebase-merge buttons), no "Update with merge" option in the Update branch dropdown
- updated `drift_signals.md` agent memory: trimmed `brew` from the weekly-draft-PR row, noted the workflow's retirement

Session continued — paper-cuts the new pipeline surfaced about itself:

- 4b21a90 docs(standups): seed daily log + chezmoi-ignore the directory (#42)
  - first commit of `standups/`; added the dir to `.chezmoiignore` so chezmoi doesn't try to mirror it into `~/standups/`
- b90c0f3 chore: delete orphan brew-outdated report (#39 follow-up) (#43)
  - `.github/brew-outdated-report.md` was a leftover from the now-retired `update-brew.yml` workflow; deleted
- 27e5f40 chore(secrets): rotate age recipient key (#44)
  - precautionary rotation: `age1wz9leqr…m4aue` → `age1lmarphzks…palg6h`. Both encrypted blobs (`encrypted_private_dot_zshrc.local.age`, `dot_aws/encrypted_private_config.age`) re-encrypted; underlying secrets unchanged
  - improvements over the runbook: `mktemp -d` + trap for plaintext (never lands in source dir); `cmp`-based round-trip verification before destructive swap
  - manual follow-up done same session: backed up the new key to Dashlane as a Secure Note; `rm ~/.config/chezmoi/key.txt.old`
- f45fcc6 docs(runbook): add 'Back up the age key' section (#45)
  - dedicated section in `docs/runbooks/secret-rotation.md`: four backup strategies (password manager / passphrase-encrypted cloud / hardware USB / paper print), recovery walkthrough with `age-keygen -y` sanity check, anti-patterns
  - also fixed the runbook's `shred -u` recommendation (misleading on APFS — `rm` is sufficient given TRIM)
- 4ec70f8 fix(drift): stop counting chezmoi warnings as drifted files (#46)
  - `HOME_DRIFT` was counting stderr from `chezmoi status` (including `config file template has changed` warnings) as drift; tightened the grep to chezmoi's `XY filename` format
  - surfaced today by #44 editing `.chezmoi.toml.tmpl` and the resulting warning showing up as "1 thing need attention" with no real drift
- 9c04963 feat(zshrc): refresh drift cache after chezmoi apply (#47)
  - extends the existing brew/mas wrapper pattern: backgrounded `chezmoi-drift-check --full --quiet` after each `chezmoi apply` so the banner reflects post-apply state instead of staying stale until TTL expires (default 4h)

Other:

- merged PR #24 — the May 18 oh-my-zsh externals bump that had been sitting open the whole time. Auto-rebased cleanly by the workflow we shipped in #40 on its way through.
- added `BOT_PAT` repo secret (fine-grained PAT, Contents/Pull requests/Workflows R+W). Validated end-to-end: PR #44 was auto-rebased, CI auto-triggered (no `gh run rerun` needed), squash-merged.
- created `action_required_bot_token.md` agent memory: diagnostic for when a workflow run shows `action_required` instead of a real failure (root cause is the GitHub_TOKEN anti-recursion guard; fix is verifying `BOT_PAT` is set/unexpired).
- auto-rebase workflow ran 7 times in production, all successful. Final run hit the no-op codepath (zero open same-repo PRs) — useful empty-input coverage.

End-of-session state: zero open PRs; working tree clean; new age key active and backed up in Dashlane; old key removed; drift banner clean (`drift: clean`); active recipient on disk is `age1lmarphzks…palg6h`.
