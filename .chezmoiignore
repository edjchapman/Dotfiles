# Files in this list live in the source repo but are NOT deployed to $HOME.
# Repo metadata, agentic scaffolding, CI, lint configs, and docs all belong here.

# Repo metadata
.github
.gitattributes
.gitleaks.toml
LICENSE
Makefile
README.md
CLAUDE.md
AGENTS.md
CONTRIBUTING.md
CODE_OF_CONDUCT.md
SECURITY.md
CHANGELOG.md

# Templates that are rendered/consumed by scripts, not deployed directly
Brewfile.tmpl

# Agentic scaffolding (project-scoped Claude Code config — not for $HOME)
.claude

# Linter/formatter configs that apply to this repo only
.editorconfig
.markdownlint-cli2.yaml
.pre-commit-config.yaml
.yamllint.yaml

# Documentation
docs

# Branding, demo, and architecture assets used by the README and Pages site —
# committed for review and serving, but never deployed to $HOME.
assets

# bats-core unit tests (run by make test-bats / CI; not deployed to $HOME)
tests

# Runtime cache populated by oh-my-zsh on shell startup — not dotfile state.
.oh-my-zsh/cache/**

# Daily standup logs written into the source dir by the standup skill — kept in
# the repo for posterity, but not deployed to $HOME.
standups
